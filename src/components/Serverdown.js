import React from "react";

export const Serverdown = () => {
  return (
    <div
      style={{
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        height: "30rem",
        color: "white",
      }}
    >
      <div>Sorry for Inconvenience, We are down at the moment</div>
    </div>
  );
};
